print("Программа, которая создаёт список из нечётных чисел от 1 до n ")
number = int(input("Введите число n (n ≥ 1): "))
odd_numbers = []
for i in range(1, n + 1):
    if i % 2 != 0:
        odd_numbers.append(i)

print(f"Список нечётных чисел от 1 до {n}: {odd_numbers}")
