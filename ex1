print("Программа, которая создаёт список из нечётных чисел от 1 до n ")
n = int(input("Введите число n (минимум 1): "))
odd_numbers = []
for i in range(1, n + 1):
    if i % 2 != 0:
        odd_numbers = odd_numbers + [i]
print(f"Список нечетных чисел от 1 до {n}: {odd_numbers}")
